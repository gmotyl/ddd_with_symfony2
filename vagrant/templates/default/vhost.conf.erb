# Override the Apache User (vhosts are included at the end of apache2.conf)
User vagrant
Group vagrant

<VirtualHost *:80>
  Servername symfony-rad.lo
  DocumentRoot /vagrant/web
  <Directory "/vagrant/web">
    AllowOverride All
    Options FollowSymLinks
  </Directory>
</VirtualHost>
